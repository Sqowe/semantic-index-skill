# AI Behavior Contract

This is a **Python SKILL project** — a portable AI tool package for semantic code indexing.

## Rules

- Always use `AI*.md` files for coding rules
  - `AI.md` — General Python conventions
  - `AI_SKILL.md` — SKILL-specific conventions (CLI output, config, Tree-sitter, LanceDB)
- Check previous talks and implementations in `docs/chats/` directory
- Check documentation in root and `docs/` directories
- Read `ARCHITECTURE.md` for system design and implementation plan
- **Never start code right after USER question/message. Propose solution and ask for explicit request for implementation**
